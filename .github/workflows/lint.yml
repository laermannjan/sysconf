---
name: Lint

on:
  push:
    branches: [main]
    paths: ["**/*.sh"]
  pull_request:
    paths: ["**/*.sh"]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run shellcheck
        run: shellcheck -x sysconf.sh setup/*.sh
