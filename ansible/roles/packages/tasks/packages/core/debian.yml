---
- name: Install core packages
  become: yes
  ansible.builtin.apt:
    name: "{{ item }}"
  loop:
    - build-essential
    - coreutils
    - keychain
    - lz4
    - man-db
    - moreutils
    - p7zip
    - unzip

