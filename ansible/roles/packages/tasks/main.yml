- name: Ensure Homebrew is installed and configured
  include_tasks: install_homebrew.yml
- name: Configure Homebrew in shell environment
  include_tasks: configure_shellenv.yml
- name: Install formulae and casks
  include_tasks: install_packages.yml
